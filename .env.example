# Port the server listens on
PORT=3000

# Optional. When set, ADLAA stores applications in MongoDB.
# When empty or unreachable, ADLAA automatically falls back to a local
# JSON file store (data/applications.json) so the app always runs.
# MONGODB_URI=mongodb://localhost:27017/adlaa
